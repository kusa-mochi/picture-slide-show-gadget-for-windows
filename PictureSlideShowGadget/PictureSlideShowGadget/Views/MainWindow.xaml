<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:prism="http://prismlibrary.com/"
        xmlns:ViewModels="clr-namespace:PictureSlideShowGadget.ViewModels" x:Class="PictureSlideShowGadget.Views.MainWindow"
        prism:ViewModelLocator.AutoWireViewModel="True"
        Title="{Binding Title}" AllowsTransparency="True" WindowStyle="None" ResizeMode="NoResize" Background="Black" Width="400" Height="300" BorderBrush="#FF585858" BorderThickness="1">
    <Window.Effect>
        <DropShadowEffect/>
    </Window.Effect>
    <Window.DataContext>
        <ViewModels:MainWindowViewModel/>
    </Window.DataContext>
    <Grid>
        <Image Source="{Binding ImageData}"/>
    </Grid>
</Window>
